# תוכנית בינה מלאכותית — SocialSniper AI Marketing Engine

> **מה זה:** תוכנית מלאה לשדרוג 5 האגנטים הקיימים (TG, IG, FB, TT, Shop) ממכונות שיווק סטטיות → מנועי שיווק חכמים עם AI
> **איפה לשמור:** `C:\Users\danin\SocialMax\תוכנית בינה מלאכותית.md`
> **בסדר ביצוע:** Phase 0 (תשתית) → Phase 1 (Warmup) → Phase 2 (AI Messages) → Phase 3 (Competitor Intel) → Phase 4 (מטורף)

---

## 📚 חלק א' — הסבר מבני (ללמידה)

### איך הבוט עובד היום:
```
1. APScheduler מפעיל session בשעה מוגדרת
2. האגנט מוצא מטרות (קבוצות/משתמשים/פוסטים)
3. random.choice(TEMPLATES) — בוחר תבנית אקראית מרשימה
4. שולח את ההודעה
5. ישן X דקות → חוזר
```
**הבעיה:** כל ההודעות הן אותן 16 תבניות. הפלטפורמות מזהות repetition → ban.

### איך הבוט יעבוד אחרי:
```
1. APScheduler מפעיל session בשעה מוגדרת
2. AccountWarmer רץ קודם (80%: גלילה, סטוריז, לייקים)
3. האגנט מוצא מטרות
4. ai_provider.generate(prompt) — כותב הודעה ייחודית לכל מטרה
   ↳ עדיפות א': Ollama בבית (0₪, מהיר)
   ↳ עדיפות ב': Gemini Flash API (1M tokens/month חינם)
   ↳ עדיפות ג': random.choice(TEMPLATES) — fallback
5. שולח הודעה שנכתבה AI, ייחודית לכל משתמש
6. מתעד תוצאות ב-SQLite
```

### מה זה Ollama ולמה זה מושלם לנו:
- **Ollama** = תוכנה שמריצה AI models בחינם על המחשב שלך
- מדליק מודל LLM (כמו ChatGPT אבל מקומי) ב-port 11434
- API פשוט: `POST http://localhost:11434/api/generate`
- **llama3.2:3b** — מהיר, קל, 2GB RAM — מספיק לכתיבת הודעות שיווק
- **mistral:7b** — איכותי יותר, 4GB RAM — לתוכן מורכב יותר
- אפס עלות, אפס API quota, אפס privacy issues

### Priority Stack — למה 3 שכבות:
```
שכבה 1: Ollama (בית)
   ✅ 0 ₪
   ✅ מהיר (< 2 שניות)
   ✅ פרטי לחלוטין
   ❌ רק כשהמחשב בבית דלוק

שכבה 2: Gemini Flash
   ✅ 1,000,000 tokens/month חינם = ~50,000 הודעות בחינם
   ✅ זמין 24/7 מכל מקום
   ❌ דורש internet
   ❌ נגמרת המכסה = שגיאה

שכבה 3: DB Templates
   ✅ תמיד עובד
   ✅ 0 latency
   ❌ הודעות חוזרות (אבל fallback בלבד)
```

### Cloudflare Tunnel — למה צריך:
- **בעיה:** Ollama רץ על `localhost:11434` — לא נגיש מבחוץ
- **פתרון:** Cloudflare Tunnel יוצר "מנהרה" מאובטחת: `internet → cloudflare servers → המחשב שלך`
- **URL שמקבלים:** `https://ollama.yourdomain.com` → מפנה ל-`localhost:11434`
- **חינם לחלוטין**, HTTPS אוטומטי, ללא port forwarding ב-router
- **Header Auth:** Cloudflare חוסם כל בקשה שלא מגיעה עם הסיסמה הסודית שלנו

---

## 🏗️ חלק ב' — ארכיטקטורה חדשה

### תיקיות חדשות:
```
backendmarket/marketing-agent/
  │
  ├── shared/                      ← NEW: בסיס משותף לכל האגנטים
  │   ├── __init__.py
  │   ├── ai_provider.py           ← Ollama → Gemini → DB (3 שכבות)
  │   ├── warmer.py                ← Account warming protocol
  │   └── sentiment.py            ← זיהוי sentiment בעברית
  │
  ├── ci/                          ← NEW: Competitor Infiltrator Agent
  │   ├── __init__.py
  │   ├── main.py                  ← scheduler + main loop
  │   ├── config.py
  │   ├── database.py
  │   ├── messages.py              ← תבניות DM ל"מתלוננים"
  │   ├── monitor.py               ← סריקת פוסטים/תגובות מתחרים
  │   ├── responder.py             ← שליחת DM + tracking
  │   └── admin.py                 ← /cistats, /cipause
  │
  ├── ig/  fb/  tt/  tg/  shop/   ← EXISTING ← שדרוג + חיבור ל-shared
```

### קובץ `.env` — כל המשתנים החדשים:
```env
# ===== AI PROVIDER =====
OLLAMA_BASE_URL=https://ollama.yourdomain.com  # Cloudflare tunnel URL
OLLAMA_SECRET=your_secret_header_key           # Cloudflare Header Auth
OLLAMA_MODEL=llama3.2:3b                       # Model to use
GEMINI_API_KEY=your_gemini_api_key             # מ-aistudio.google.com

# ===== COMPETITOR INFILTRATOR =====
CI_COMPETITORS_TG=@competitor1,@competitor2    # Telegram channels לניטור
CI_COMPETITORS_IG=competitor_ig1,competitor2   # Instagram usernames
DAILY_CI_DM_LIMIT=8                           # מקסימום DMs ביום
CI_SCAN_INTERVAL_HOURS=3

# ===== ACCOUNT WARMER =====
WARM_INTENSITY=medium                          # low/medium/high
WARM_BEFORE_SESSION=true                       # חמם לפני כל session
```

---

## 📅 חלק ג' — תוכנית עבודה יומית

---

### 🔵 יום 1 — תשתית (Phase 0)
> **מטרה:** הכל מוכן לפני שנגע באגנטים

#### משימות:
1. **התקנת Ollama** על המחשב הביתי
   ```bash
   # Windows: הורד מ-ollama.com/download
   ollama pull llama3.2:3b    # מודל קל ומהיר (2GB)
   ollama serve               # מפעיל ב-port 11434
   ```

2. **Gemini API Key** (חינם)
   - כנס ל-`aistudio.google.com`
   - Get API Key → Copy → שמור ב-.env
   - Plan: Free tier = 1,500 requests/day + 1M tokens/month

3. **Cloudflare Tunnel** (30 דקות)
   ```bash
   # Windows
   winget install Cloudflare.cloudflared
   cloudflared tunnel login
   cloudflared tunnel create socialsniper-ai
   # קשר לדומיין שלך ב-Cloudflare
   cloudflared tunnel route dns socialsniper-ai ollama.yourdomain.com
   ```

4. **Header Auth בCloudflare:**
   - Security → WAF → Custom Rules → Create Rule
   - Condition: `http.request.headers["x-internal-token"][0] ne "YOUR_SECRET"`
   - Action: Block

5. **יצירת `shared/ai_provider.py`** — מחלקת AI המרכזית
6. **יצירת `shared/warmer.py`** — פרוטוקול חימום
7. **יצירת `shared/sentiment.py`** — זיהוי מילות מפתח שליליות בעברית
8. **עדכון `requirements.txt`:** הוסף `google-generativeai`, `ollama`

---

### 🔵 יום 2 — Account Warmer לכל הפלטפורמות
> **מטרה:** כל session שיווקי מקדימה ריצת חימום אנושית

#### Instagram (`ig/outreach.py` + `ig/main.py`):
```python
# BEFORE any DM/comment session:
from shared.warmer import IGWarmer
warmer = IGWarmer(client)
await warmer.run(intensity="medium")
# Actions: watch 5 stories + like 8 random posts + scroll explore 60s
```

#### Facebook (`fb/outreach.py` + `fb/main.py`):
```python
from shared.warmer import PlaywrightWarmer
warmer = PlaywrightWarmer(page, platform="facebook")
await warmer.run()
# Actions: scroll feed 45s + hover 3 posts + watch 2 videos + react to 1 post
```

#### TikTok (`tt/outreach.py` + `tt/main.py`):
```python
from shared.warmer import PlaywrightWarmer
warmer = PlaywrightWarmer(page, platform="tiktok")
await warmer.run()
# Actions: watch 4 TikTok videos to >80% + like 3 + swipe FYP 30s
```

**Warming Levels:**
| Level | Actions | Duration | מתי |
|-------|---------|----------|-----|
| `low` | 3 actions, 2 min | חשבון ותיק |
| `medium` | 6 actions, 5 min | שימוש יומי |
| `high` | 10 actions, 12 min | חשבון חדש / אחרי ban |

---

### 🔵 יום 3 — AI Messages לTelegram + Instagram
> **מטרה:** כל הודעה שנשלחת — ייחודית, כתובה בAI

#### Telegram (`tg/poster.py`):
```python
# לפני: text = random.choice(TEMPLATES)
# אחרי:
from shared.ai_provider import AIProvider
ai = AIProvider()

prompt = f"""
כתוב הודעת שיווק קצרה בעברית (2-3 שורות) לקבוצת טלגרם בשם "{group_name}".
נושא: שירות SMM ישראלי (עוקבים/לייקים/צפיות).
טון: {random.choice(['חברי וישיר', 'מקצועי', 'קצר ולעניין', 'עם הומור'])}
אל תכלול: אימוג׳ים מרובים, הבטחות מוגזמות, קישורים.
אורך: מקסימום 3 שורות.
"""
text = await ai.generate(prompt, scenario="tg_post")
```

#### Instagram DM (`ig/outreach.py`):
```python
prompt = f"""
כתוב DM קצר בעברית לאינסטגרם.
פרטי הנמען: שם="{username}", ביו="{bio[:100]}", {followers}K עוקבים.
מטרה: הצע שירות SMM - עוקבים/לייקים לאינסטגרם.
טון: לא spam, חברי, מכבד, ישיר.
מקסימום: 2 משפטים + CTA.
"""
```

#### Instagram Comment (`ig/outreach.py`):
```python
prompt = f"""
כתוב תגובה קצרה (1 משפט) בעברית לפוסט אינסטגרם.
נושא הפוסט: "{caption[:150]}"
מטרה: תגובה אמיתית שמעידה שקראת + mention subtle של SocialSniper.
אסור: spam, פרסום ישיר, @username מרובים.
"""
```

---

### 🔵 יום 4 — AI Messages לFacebook + TikTok
> **מטרה:** כל פוסט/תגובה/DM בFB ובTT — ייחודי וחכם

#### Facebook Group Post (`fb/groups.py`):
```python
prompt = f"""
כתוב פוסט בעברית לקבוצת פייסבוק בשם "{group_name}" ({member_count} חברים).
מוצר: שירות SMM ישראלי - עוקבים/לייקים בכל הפלטפורמות.
כולל: שאלה שפותחת שיח, ערך אמיתי לקורא, CTA עדין.
אסור: שפה מכירתית מדי, הבטחות שווא, לינקים.
אורך: 3-5 שורות.
"""
```

#### Facebook Comment Reply (`fb/outreach.py`):
```python
prompt = f"""
ראיתי תגובה בפייסבוק: "{comment_text}"
כתוב תגובה בעברית שמגיבה ישירות לתוכן + מזכירה בעדינות ש-SocialSniper יכול לעזור.
"""
```

#### TikTok Comment (`tt/outreach.py`):
```python
prompt = f"""
כתוב תגובה לסרטון TikTok.
כותרת הסרטון: "{video_desc[:100]}"
מטרה: תגובה מצחיקה/מעניינת שמעלה אלגוריתם + mention של SocialSniper.
אורך: 1 משפט קצר.
"""
```

---

### 🔵 יום 5 — Competitor Infiltrator: Monitor
> **מטרה:** לזהות אנשים שמבטאים אי שביעות רצון מהמתחרים

#### `ci/monitor.py` — סריקה בכל פלטפורמה:

**Telegram (Telethon):**
```python
# סרוק הודעות ב-channels של מתחרים
async for message in client.iter_messages(competitor_channel, limit=200):
    if sentiment.is_complaint(message.text):
        db.add_target(user_id=message.sender_id, platform="telegram",
                      complaint=message.text, channel=competitor_channel)
```

**Instagram (Instagrapi):**
```python
# קבל תגובות על פוסטים של מתחרים
comments = client.media_comments(competitor_media_id)
for comment in comments:
    if sentiment.is_complaint(comment.text):
        db.add_target(user_id=comment.user.pk, platform="instagram",
                      complaint=comment.text)
```

**Facebook (Playwright):**
```python
# סרוק תגובות על פוסטים בקבוצות SMM
# Playwright → navigate to competitor page → extract comments
comments = await page.query_selector_all('[data-testid="comment"]')
for c in comments:
    text = await c.inner_text()
    if sentiment.is_complaint(text):
        db.add_target(...)
```

#### `ci/sentiment.py` — זיהוי תלונות בעברית:
```python
COMPLAINT_KEYWORDS = [
    # מחיר
    "יקר מדי", "יקר מאוד", "לא שווה", "יקר לי",
    # שירות גרוע
    "גרוע", "מאוכזב", "לא מומלץ", "שירות גרוע",
    # לא עבד
    "לא הגיעו", "לא עובד", "נפלו", "ירדו", "נפל",
    # חיפוש חלופה
    "מחפש חלופה", "יש משהו טוב", "מישהו יודע",
    "מה הכי טוב", "מה עדיף", "המלצה על",
    # כעס
    "בוטים", "מרמה", "רמאות", "לא אמין", "לא כדאי",
]

def is_complaint(text: str, threshold: int = 1) -> bool:
    text_lower = text.lower()
    hits = sum(1 for kw in COMPLAINT_KEYWORDS if kw in text_lower)
    return hits >= threshold
```

---

### 🔵 יום 6 — Competitor Infiltrator: Responder + Admin
> **מטרה:** לשלוח DM חכם לכל מי שמתלונן

#### `ci/responder.py` — AI-generated DM לפי הפלטפורמה:

**Telegram:**
```python
async def dm_telegram_target(client, target):
    prompt = f"""
    מישהו כתב ב-Telegram: "{target.complaint}"
    כתוב לו הודעה פרטית בעברית שמציעה SocialSniper כחלופה.
    היה: אמפתי, לא spam, מציע ערך ממשי.
    כולל: הצעה קונקרטית + link לבוט @socialsniper93_bot
    אסור: להגיד "ראיתי שאתה מתלונן"
    """
    text = await ai.generate(prompt, scenario="ci_dm")
    await client.send_message(target.user_id, text)
    db.mark_sent(target.id)
```

**Instagram:**
```python
async def dm_instagram_target(ig_client, target):
    prompt = f"""
    מישהו עם {target.followers}K עוקבים באינסטגרם כתב: "{target.complaint}"
    שלח לו DM שמציע SocialSniper. להיות ספציפי לסוג הבעיה שלו.
    """
    text = await ai.generate(prompt, scenario="ci_dm")
    ig_client.direct_send(text, user_ids=[target.user_id])
```

#### `ci/admin.py`:
```
/cistats  → כמה תלונות נמצאו היום, כמה DM נשלחו, conversion rate
/cipause  → עצור את הסריקה
/ciresume → חזור לסריקה
/cicompetitors → רשימת המתחרים שמנוטרים
/ciconfig → הגדרות (keyword threshold, daily limit)
```

---

### 🔵 יום 7 — Integration + בדיקות + Prompt Engineering
> **מטרה:** הכל עובד יחד, הודעות AI מוצלחות

#### Checklist יום 7:
- [ ] בדוק Ollama זמין: `curl https://ollama.yourdomain.com/api/tags`
- [ ] בדוק Gemini fallback (כבה Ollama → וודא שעובר ל-Gemini)
- [ ] בדוק DB fallback (כבה גם Gemini → וודא templates מתוצאות)
- [ ] הרץ CI agent לדקה ← בדוק שהוא מוצא תלונות ב-Telegram
- [ ] בדוק שWarmer לא גורם לחשבון Instagram להתחבר מחדש
- [ ] עשה A/B: שלח 5 הודעות static vs 5 AI → השווה responses

#### Prompt Engineering — טיפים:
```python
# הוסף always: language + tone + constraints
SYSTEM_PROMPT = """
אתה כותב הודעות שיווק בעברית לעסק SMM ישראלי.
חוקים:
1. תמיד בעברית
2. אף פעם לא גנרי — תמיד ספציפי לנמען
3. מקסימום 3 שורות
4. טון: אנושי ואמיתי, לא sales-y
5. אסור: סימני קריאה מרובים, מילים כמו "מדהים!!" / "חבל לפספס"
"""
```

---

## 🚀 חלק ד' — "מפלצת שיווקית" לכל פלטפורמה

### Telegram — AI Super Agent:
| פיצ'ר | תיאור | קובץ |
|-------|-------|------|
| Smart Group Scoring | AI מדרג קבוצות לפי relevance | `tg/scoring.py` |
| Dynamic Templates | AI כותב הודעה ייחודית לכל קבוצה | `tg/poster.py` |
| Competitor Intel | ניטור channels מתחרים | `ci/monitor.py` |
| Response Handler | כשמישהו עונה → AI מגיב חכם | `tg/responder.py` |
| Timing Optimizer | AI מזהה שעות שיא לכל קבוצה | `tg/scheduler.py` |

### Instagram — AI Super Agent:
| פיצ'ר | תיאור | קובץ |
|-------|-------|------|
| Profile Analyzer | AI קורא ביו → מחליט אם שווה DM | `ig/profiler.py` |
| Caption Generator | AI כותב caption לReels | `ig/content.py` |
| Smart Comments | AI כותב תגובה רלוונטית לפוסט | `ig/outreach.py` |
| Account Warmer | 80% human / 20% marketing | `shared/warmer.py` |
| Complaint Detector | ניטור comments של מתחרים | `ci/monitor.py` |

### Facebook — AI Super Agent:
| פיצ'ר | תיאור | קובץ |
|-------|-------|------|
| Group Post Customizer | AI כותב פוסט ייחודי לכל קבוצה | `fb/groups.py` |
| Comment Intent Classifier | AI מחליט אם לDM אחרי תגובה | `fb/outreach.py` |
| Messenger Personalization | AI כותב DM לפי פרופיל | `fb/outreach.py` |
| Page Post AI | AI כותב תוכן לדף הפייסבוק שלנו | `fb/page.py` |
| FB Warmer | Scroll + react + watch | `shared/warmer.py` |

### TikTok — AI Super Agent:
| פיצ'ר | תיאור | קובץ |
|-------|-------|------|
| Video Description AI | AI כותב תיאור ל-TikTok videos | `tt/content.py` |
| Hashtag Intelligence | AI בוחר hashtags לפי trending | `tt/discovery.py` |
| Comment Contextualizer | AI כותב תגובה רלוונטית לכל video | `tt/outreach.py` |
| FYP Optimizer | Warmer שמכוון FYP לנישה שלנו | `shared/warmer.py` |
| Follow Scorer | AI מחליט מי שווה לfolllow | `tt/outreach.py` |

### Competitor Infiltrator — כל הפלטפורמות:
| פלטפורמה | שיטת ניטור | שיטת תגובה |
|----------|-----------|-----------|
| Telegram | iter_messages על channels מתחרים | send_message PM |
| Instagram | media_comments על posts מתחרים | direct_send |
| Facebook | Playwright → scrape comments | Messenger DM |
| TikTok | Playwright → scrape comments | follow + comment |

---

## 📋 חלק ה' — קבצים שייכתבו / ישתנו

### קבצים חדשים:
| קובץ | שורות משוערות | מטרה |
|------|--------------|-------|
| `shared/__init__.py` | 5 | package init |
| `shared/ai_provider.py` | 120 | AI priority stack |
| `shared/warmer.py` | 200 | Account warming |
| `shared/sentiment.py` | 60 | Hebrew sentiment |
| `ci/__init__.py` | 5 | package init |
| `ci/main.py` | 80 | scheduler + runner |
| `ci/config.py` | 40 | env loading |
| `ci/database.py` | 100 | SQLite tables |
| `ci/messages.py` | 80 | DM templates |
| `ci/monitor.py` | 150 | multi-platform scan |
| `ci/responder.py` | 100 | AI DM sender |
| `ci/admin.py` | 80 | /cistats etc. |

### קבצים שמשתנים:
| קובץ | שינוי |
|------|-------|
| `requirements.txt` | + google-generativeai, ollama |
| `.env.example` | + 8 משתנים חדשים |
| `start.sh` | + python -m ci.main |
| `shop/main.py` | + import ci.admin handlers |
| `ig/outreach.py` | + warmer + ai_provider |
| `ig/main.py` | + warm before sessions |
| `fb/outreach.py` | + warmer + ai_provider |
| `fb/groups.py` | + ai_provider for post content |
| `fb/main.py` | + warm before sessions |
| `tt/outreach.py` | + warmer + ai_provider |
| `tt/main.py` | + warm before sessions |
| `tg/poster.py` | + ai_provider for messages |

---

## ✅ Verification

```bash
# 1. בדוק Ollama
curl https://ollama.yourdomain.com/api/generate \
  -H "X-Internal-Token: YOUR_SECRET" \
  -d '{"model":"llama3.2:3b","prompt":"כתוב DM שיווקי קצר בעברית","stream":false}'

# 2. בדוק Gemini
python -c "
import google.generativeai as g
g.configure(api_key='YOUR_KEY')
print(g.GenerativeModel('gemini-1.5-flash').generate_content('שלום').text)
"

# 3. בדוק ai_provider
python -c "
from shared.ai_provider import AIProvider
import asyncio
ai = AIProvider()
print(asyncio.run(ai.generate('כתוב DM שיווקי', 'test')))
"

# 4. בדוק warmer (Instagram)
python -c "from shared.warmer import IGWarmer; print('Warmer OK')"

# 5. הרץ ci agent בבדיקה
python -m ci.main --dry-run  # סרוק אבל אל תשלח

# 6. start.sh — הרץ הכל
chmod +x start.sh && ./start.sh
tail -f logs/ci_agent.log
```

---

> **סדר יצירת הקבצים:**
> `shared/` (יום 1) → `ig/fb/tt warmer` (יום 2) → `tg/ig AI messages` (יום 3) → `fb/tt AI messages` (יום 4) → `ci/monitor` (יום 5) → `ci/responder + admin` (יום 6) → `integration + tests` (יום 7)
